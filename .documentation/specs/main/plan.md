# Implementation Plan: Audit Remediation (2026-03-16)

**Branch**: `main` | **Date**: 2026-03-16 | **Audit**: `.documentation/copilot/audit/2026-03-16_results.md`
**Input**: Findings from site audit — Immediate Actions (HIGH) and Medium Priority items.

---

## Summary

Addresses 13 findings from the 2026-03-16 site audit across three HIGH-priority and ten MEDIUM-priority items. Work is grouped into four tracks that can progress independently: (1) CI infrastructure, (2) test coverage, (3) source hardening and security, and (4) code decomposition. No new domain model is introduced. All changes must preserve constitution layering and pass `dotnet build` + `dotnet test`.

---

## Technical Context

**Language/Version**: C# / .NET 10
**Primary Dependencies**: ASP.NET Core MVC, MSTest 4.1.0, GitHub Actions, coverlet.collector 8.0.0
**Storage**: File-backed JSON under `RESTRunner.Web/Data` (unchanged)
**Testing**: MSTest — extending to new projects `RESTRunner.Web.Tests` and `RESTRunner.Services.Tests`
**Target Platform**: ASP.NET Core web app + console runner on Windows/Linux/macOS
**Project Type**: Multi-project .NET solution
**Performance Goals**: No change
**Constraints**: Thread safety unchanged; layer boundaries must not be crossed; no new external dependencies unless MSTest pattern libraries needed
**Scale/Scope**: 6 source projects → 8 after new test projects; no runtime behavior change

---

## Constitution Check

*GATE: Checked before Phase 0. Re-checked after Phase 1 design.*

| Check | Status | Notes |
|-------|--------|-------|
| Layering preserved | ✅ | `DomainConstants` added to Domain; new test projects reference only their target layer |
| C# defaults preserved | ✅ | All new projects: nullable enable, implicit usings enable, net10.0 |
| Test impact defined | ✅ | Two new MSTest projects; initial test scope documented in research.md |
| Logging impact defined | ✅ | No logging changes; `ExecutionHistoryService` extraction keeps ILogger injection in each class |
| Public-surface documentation impact | ✅ | `ExecutionApiController` split preserves all existing XML doc; `DebugEdit` gets doc update |
| Boundary validation impact | ✅ | `DebugEdit` gains `[Authorize]`; no other input surface changes |
| Validation commands | ✅ | `dotnet build`, `dotnet test` (new projects auto-discovered by test runner) |

---

## Project Structure

### Documentation (this feature)

```text
specs/main/
├── plan.md          ← this file
├── research.md      ← Phase 0 output (CI patterns, test structure, decomposition decisions)
├── data-model.md    ← Phase 1 output (new types, interfaces, test projects)
└── tasks.md         ← Phase 2 output (generated by /speckit.tasks — NOT yet created)
```

### Source Code Changes

```text
.github/
└── workflows/
    └── build.yml                              [NEW] — CI: build + test on push/PR

RESTRunner.Domain/
└── Constants/
    └── DomainConstants.cs                     [NEW] — SamplePassword constant

RESTRunner/
└── Extensions/
    └── CompareRunner_Extensions.cs:30         [EDIT] — replace "password" with DomainConstants.SamplePassword

RESTRunner.Web/
├── Program.cs:144,424,435                     [EDIT] — replace hardcoded passwords with DomainConstants.SamplePassword
├── Controllers/
│   ├── ConfigurationController.cs:282,452     [EDIT] — [Authorize] on DebugEdit; DomainConstants.SamplePassword
│   ├── ExecutionController.cs                 [EDIT] — strip API endpoints; MVC view actions only
│   └── ExecutionApiController.cs             [NEW] — ControllerBase; receives API endpoints from above
└── Services/
    ├── FileOpenApiService.cs                  [EDIT] — strip parsing region (~300 lines remain)
    ├── OpenApiSpecParser.cs                   [NEW] — extracted parsing logic
    ├── IExecutionHistoryService.cs            [NEW] — persistence interface
    ├── ExecutionHistoryService.cs             [NEW] — implements IExecutionHistoryService
    └── RealExecutionService.cs                [EDIT] — removes persistence region; delegates to IExecutionHistoryService

RESTRunner.Web.Tests/
├── RESTRunner.Web.Tests.csproj               [NEW]
├── GlobalUsings.cs                           [NEW]
├── Controllers/
│   ├── ExecutionControllerTests.cs           [NEW]
│   └── ConfigurationControllerTests.cs       [NEW]
└── Services/
    └── RealExecutionServiceTests.cs          [NEW]

RESTRunner.Services.Tests/
├── RESTRunner.Services.Tests.csproj          [NEW]
├── GlobalUsings.cs                           [NEW]
└── ExecuteRunnerServiceTests.cs              [NEW]
```

---

## Complexity Tracking

No constitution violations. All changes simplify or extend the existing structure.

---

## Phase 0: Research (Complete)

See `research.md`. All unknowns resolved:

| Unknown | Resolution |
|---------|------------|
| CI runner choice | GitHub Actions `ubuntu-latest`, `.github/workflows/build.yml` |
| SampleCRUDClient.cs decomposition | EXCLUDED — NSwag auto-generated, constitution Engineering Standards §4 applies |
| Test project pattern | Mirror `RESTRunner.Domain.Tests.csproj` exactly (MSTest 4.1.0, coverlet 8.0.0) |
| Password constant location | New `RESTRunner.Domain/Constants/DomainConstants.cs` |
| FileOpenApiService split boundary | CRUD vs parsing — `OpenApiSpecParser.cs` extraction |
| ExecutionController split boundary | MVC view actions vs `[HttpGet/Post("/api/...")]` — already delimited by comment headers |
| RealExecutionService split boundary | Execution orchestration vs history persistence — new `IExecutionHistoryService` |
| DebugEdit fix | Add `[Authorize]` attribute; verify action is not exposed to anonymous routes |
| Program.cs BUG comment | Implement missing `GET /api/configurations` or move to `ConfigurationApiController` |

---

## Phase 1: Design (Complete)

See `data-model.md` for all new types and interface contracts.

---

## Phase 2: Implementation Tracks

> Full task breakdown generated by `/speckit.tasks`. Below is the ordered delivery plan.

### Track A — CI Infrastructure (ARCH1)

**Prerequisite**: None.

| Step | File | Action |
|------|------|--------|
| A1 | `.github/workflows/build.yml` | Create GitHub Actions workflow: checkout, setup-dotnet 10.x, restore, build, test |
| A2 | Verify locally | Run `dotnet build` and `dotnet test` from repo root; confirm all projects pass before pushing |

**Definition of Done**: `build.yml` exists; every push to `main` triggers all build and test steps without failure.

---

### Track B — Test Coverage (TEST1, TEST2)

**Prerequisite**: Track A complete (CI must catch regressions from new test projects).

| Step | File | Action |
|------|------|--------|
| B1 | `RESTRunner.Services.Tests/RESTRunner.Services.Tests.csproj` | New MSTest project; net10.0, nullable, implicit usings, MSTest 4.1.0, coverlet 8.0.0; references `RESTRunner.Services.HttpClientRunner.csproj` and `RESTRunner.Domain.csproj` |
| B2 | `RESTRunner.Services.Tests/GlobalUsings.cs` | Global using for MSTest + domain namespaces |
| B3 | `RESTRunner.Services.Tests/ExecuteRunnerServiceTests.cs` | Tests: path substitution in `GetResponseAsync`, body template substitution, null request guard |
| B4 | `RESTRunner.Web.Tests/RESTRunner.Web.Tests.csproj` | New MSTest project; net10.0, nullable, implicit usings, MSTest 4.1.0, coverlet 8.0.0; references `RESTRunner.Web.csproj`; add `Microsoft.AspNetCore.Mvc.Testing` |
| B5 | `RESTRunner.Web.Tests/GlobalUsings.cs` | Global using for MSTest + web + domain namespaces |
| B6 | `RESTRunner.Web.Tests/Controllers/ExecutionControllerTests.cs` | Tests: Index returns view, Results returns view for valid id, Results redirects for missing id |
| B7 | `RESTRunner.Web.Tests/Controllers/ConfigurationControllerTests.cs` | Tests: Index returns view, Create (GET) returns view |
| B8 | `RESTRunner.Web.Tests/Services/RealExecutionServiceTests.cs` | Tests: StartExecutionAsync throws for unknown config |
| B9 | Solution file | Add both new `.csproj` files to `RESTRunner.sln` |

**Definition of Done**: `dotnet test` discovers and passes all tests in new projects; no existing tests broken.

---

### Track C — Source Hardening & Security (SEC1–SEC5, QUAL5, QUAL6)

**Prerequisite**: None — all changes are isolated edits.

| Step | File | Action |
|------|------|--------|
| C1 | `RESTRunner.Domain/Constants/DomainConstants.cs` | Create file; define `public const string SamplePassword = "[sample]"` |
| C2 | `RESTRunner/Extensions/CompareRunner_Extensions.cs:30` | Replace `"password"` with `DomainConstants.SamplePassword`; add using |
| C3 | `RESTRunner.Web/Program.cs:144` | Replace `Password = "password"` with `Password = DomainConstants.SamplePassword` |
| C4 | `RESTRunner.Web/Program.cs:424` | Replace `Password = "password"` with `Password = DomainConstants.SamplePassword` |
| C5 | `RESTRunner.Web/Program.cs:435` | Replace `Password = "admin123"` with `Password = DomainConstants.SamplePassword` |
| C6 | `RESTRunner.Web/Controllers/ConfigurationController.cs:452` | Replace `Password = "password"` with `Password = DomainConstants.SamplePassword` |
| C7 | `RESTRunner.Web/Controllers/ConfigurationController.cs:282` | Add `[Authorize]` attribute to `DebugEdit`; update XML doc to reflect authorization requirement |
| C8 | `RESTRunner.Web/Program.cs:335` | Implement or register the missing `GET /api/configurations` endpoint; remove `// BUG:` comment |
| C9 | `RESTRunner.Domain.Tests/Extensions/CompareUser_ExtensionsTests.cs:19,46` | Define `private const string TestPassword = "TestPassword"` inside the test class; use it in both assertions |

**Definition of Done**: `dotnet build` shows no warnings about the changed files; no literal `"password"` or `"admin123"` strings remain in non-test production code; `// BUG:` comments removed.

---

### Track D — Code Decomposition (QUAL2, QUAL3, QUAL4)

**Prerequisite**: Track B complete (new tests must cover extracted units).

| Step | File | Action |
|------|------|--------|
| D1 | `RESTRunner.Web/Services/OpenApiSpecParser.cs` | Extract parsing region from `FileOpenApiService.cs`; make all methods `internal static`; no DI registration |
| D2 | `RESTRunner.Web/Services/FileOpenApiService.cs` | Remove extracted parsing methods; call `OpenApiSpecParser.*` where previously inline; verify ~300 lines remain |
| D3 | `RESTRunner.Web/Services/IExecutionHistoryService.cs` | Define interface: SaveHistoryAsync, GetHistoryAsync, GetAllHistoryAsync, DeleteHistoryAsync |
| D4 | `RESTRunner.Web/Services/ExecutionHistoryService.cs` | Implement `IExecutionHistoryService`; move file-persistence region from `RealExecutionService.cs`; inject `IFileStorageService` and `ILogger` |
| D5 | `RESTRunner.Web/Services/RealExecutionService.cs` | Remove persistence region; inject and use `IExecutionHistoryService`; verify ~300 lines remain |
| D6 | `RESTRunner.Web/Program.cs` (DI registration) | Register `ExecutionHistoryService` as `IExecutionHistoryService` scoped |
| D7 | `RESTRunner.Web/Controllers/ExecutionApiController.cs` | New `ControllerBase` class; move all `[HttpGet/Post("/api/execution/...")]` actions from `ExecutionController` |
| D8 | `RESTRunner.Web/Controllers/ExecutionController.cs` | Remove migrated API actions; retain only MVC view actions; verify ~200 lines remain |
| D9 | `dotnet build && dotnet test` | Confirm no regressions; all 4 test projects pass |

**Definition of Done**: 4 files that previously exceeded 500 lines each rendered under 350 lines; no behaviour change; all tests pass.

---

## Delivery Order Summary

```
Track A (CI)      → independent; deliver first so CI catches subsequent regressions
Track C (Harden)  → independent; deliver in parallel with A
Track B (Tests)   → after A (CI must be in place); deliver before D
Track D (Decomp)  → after B (need test coverage before refactoring)
```

```
Day 1: A1, A2, C1–C9
Day 2: B1–B9
Day 3: D1–D9, final dotnet build + dotnet test
```

---

## Validation Commands

```bash
# From repo root after each Track:
dotnet build
dotnet test

# Verify no literal passwords remain in production code:
grep -rn '"password"\|"admin123"' --include="*.cs" \
  --exclude-dir="*Tests*" --exclude-dir="obj" .
```

---

## Post-Delivery Audit Check

After all tracks complete, re-run `/speckit.site-audit` to confirm:
- ARCH1, TEST1, TEST2, SEC1–SEC5, QUAL2–QUAL4, QUAL5, QUAL6 → resolved
- Constitution Compliance score improves from 74% → ≥90%
- Overall Health → HEALTHY
