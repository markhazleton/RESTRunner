# Constitution Amendment Proposal: CAP-2026-001

## Proposal Metadata

- **Proposal ID**: CAP-2026-001
- **Created**: 2026-03-25T22:49:31Z
- **Status**: APPLIED
- **Proposer**: /speckit.evolve-constitution
- **Source**: from-audit

## Amendment Type

Select one:
- [x] **ADD** - New principle
- [ ] **MODIFY** - Update existing principle
- [ ] **DEPRECATE** - Remove or soften principle
- [ ] **CLARIFY** - Add examples/guidance without changing rules

## Current State

No existing principle directly governs secret handling or executable-source
credential defaults. Principle V addresses boundary validation and
maintainability, but it does not define repository rules for secrets,
placeholders, or environment-backed configuration.

## Evidence

### Pattern Analysis

The current constitution does not explicitly prohibit hardcoded credentials in
runtime code, yet the latest codebase audit found repeated executable-source
password defaults across console and web entry paths. This produced the only
critical audit finding and indicates a governance gap rather than a single-file
implementation mistake.

| Source | Finding | Count |
|--------|---------|-------|
| Audit Reports | Hardcoded secrets or credential defaults in executable code | 7 occurrences |
| Current Repository Scan | `Password = "changeme"` in non-test executable paths | 5 occurrences |

### Specific Examples

1. **Audit 2026-03-20**: `RequestSpark/Extensions/CompareRunner_Extensions.cs:30` was flagged as a critical hardcoded credential path.
2. **Audit 2026-03-20**: `RequestSpark.Web/Program.cs` contained repeated password defaults in executable setup paths.
3. **Current scan 2026-03-25**: executable-source password defaults remain in `RequestSpark.Web/Program.cs`, `RequestSpark/Extensions/CompareRunner_Extensions.cs`, and `RequestSpark.Web/Controllers/ConfigurationController.cs`.

## Proposed Change

### Principle: VI. Secure Configuration And Secret Hygiene

RequestSpark MUST keep secrets, credentials, and security-sensitive defaults out
of executable source code. Runtime configuration that requires passwords,
tokens, or connection secrets MUST come from environment variables, user input,
secret stores, or clearly non-secret placeholder values that cannot be mistaken
for deployable credentials. Sample data and developer scaffolding MUST make the
unsafe state obvious and MUST not normalize shipping hardcoded secrets.

**Rationale**: The current constitution covers architecture, conventions,
testing, logging, documentation, validation, and maintainability, but it does
not establish a direct rule for secret hygiene. The audit surfaced repeated
credential defaults and one critical finding, which is strong evidence that a
specific principle is needed.

**MUST Requirements:**
- Executable code MUST NOT embed real or plausible deployable secrets.
- Runtime credential inputs MUST come from configuration providers, secret
  stores, or explicit user-provided values.
- Sample or seeded values MUST use unmistakable non-secret placeholders and
  document how operators replace them safely.

**SHOULD Recommendations:**
- Reviews and audits SHOULD flag any new hardcoded secret-like literals outside
  isolated tests.
- Security-sensitive configuration paths SHOULD fail fast when required secrets
  are absent instead of silently falling back to defaults.

## Impact Assessment

### Affected Areas

- **Existing Code**: Low-to-medium impact; at least 3 executable-source files are
  implicated by current evidence.
- **Future Development**: New samples, seeds, and startup configuration paths
  will need explicit secret sourcing rules.
- **Tooling**: Audit and PR review prompts may need one additional compliance
  check for hardcoded secrets and placeholder quality.

### Compliance Effort

| Current Compliance | Remediation Effort | Timeline |
|--------------------|-------------------|----------|
| ~0% for explicit governance coverage | MEDIUM | 1 sprint |

### Risk Analysis

| If Adopted | If Rejected |
|------------|-------------|
| May require cleanup in sample/bootstrap code and slightly stricter review expectations | Hardcoded secret-like defaults can continue to recur without a clear blocking rule |

## Adoption Plan

### Phase 1: Soft Introduction (Recommended)

1. Add the principle with SHOULD-level review emphasis for one review cycle.
2. Replace current executable-source password defaults with documented
   placeholders or configuration-backed inputs.
3. Update audit and PR review guidance to report secret-like literals
   consistently.

### Phase 2: Full Enforcement

1. Treat new executable-source secret literals as constitution-blocking.
2. Require explicit remediation or documented exception for demo/sample flows.
3. Add repository automation or linting support if lightweight enforcement is
   practical.

## Review Checklist

- [x] Principle is testable in code review
- [x] Principle is actionable for developers
- [x] Evidence justifies the change
- [x] Impact assessment is realistic
- [x] Adoption plan is achievable

## Voting Record

| Reviewer | Vote | Date | Comments |
|----------|------|------|----------|
| /speckit.evolve-constitution | APPROVE | 2026-03-25 | Proposal approved and applied in constitution version 1.1.0 |

## Resolution

- **Decision**: APPROVED
- **Decision Date**: 2026-03-25
- **Applied in Version**: 1.1.0

---

*Generated by /speckit.evolve-constitution v1.0*
*Review period: 14 days from creation*
